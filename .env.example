# Copy to .env to override the defaults baked into docker-compose.yml.
# Keys mirror the Kubernetes ConfigMap (.claude/docs/08-deployment.md §5).

DM_LOG_LEVEL=INFO
DM_FSYNC_MODE=always
DM_SNAPSHOT_EVERY=1000
DM_BAND_REFRESH=1s
DM_RESERVATION_TTL=60s
